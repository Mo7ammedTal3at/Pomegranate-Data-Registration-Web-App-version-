
@{
    ViewBag.Title = "ImportsForPeriod";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@section styles{
    <link rel="stylesheet" href="~/Content/AdminLTE/bower_components/bootstrap-datepicker/dist/css/bootstrap-datepicker.min.css">
    @*<link rel="stylesheet" href="../../bower_components/bootstrap-datepicker/dist/css/bootstrap-datepicker.min.css">*@
}
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">الصناديق المتبقية بالخارج</h3>
                    @using (Ajax.BeginForm(new AjaxOptions { HttpMethod = "Post", UpdateTargetId = "result", InsertionMode = InsertionMode.Replace }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <div class="row">
                            
                            <div class="form-group col-md-4 col-md-offset-4 row">
                                @Html.Label("الصنف",htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-10">
                                    @Html.DropDownList("BoxTypeId", null, htmlAttributes: new { @class = "form-control" })
                                    @*@Html.ValidationMessageFor(model => model.BoxTypeId, "", new { @class = "text-danger" })*@
                                </div>
                            </div>
                        </div>
                        <input type="submit" value="تقرير" class="btn btn-default" />
                        @*<button class="btn btn-success" type="submit">تقرير</button>*@
                    }
                </div>
                <!-- /.box-header -->
                <div id="result" class="box-body">
                    @*@Html.Partial("_CustomerImports", Model);*@
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>

@section scripts{
    <script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
    <!-- bootstrap datepicker -->
    @*<script src="~/Content/AdminLTE/bower_components/bootstrap-datepicker/dist/js/bootstrap-datepicker.min.js"></script>
        <script>
        $(function () {
            //Date picker
            $('.datePicker').datepicker({
                autoclose: true
            });
            //$('#datePickerTo').datepicker({
            //    autoclose: true
            //});
        });
        </script>*@
}

